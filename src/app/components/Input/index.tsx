import React from "react";
import { View, Text } from "react-native";
import { Item, Input as NbInput } from "native-base";

const Input = () => {
  return (
    <Item>
      <NbInput />
    </Item>
  );
};

export default Input;
